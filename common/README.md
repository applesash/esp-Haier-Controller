# Shared Components

`heat_common` and `heat_modbus` are initially copied from `esp-heating-control` because their framing and testable protocol helpers are useful here. Haier-specific register maps belong in this repository and must not be added to the generic Modbus layer.
